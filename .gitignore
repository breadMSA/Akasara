node_modules/
dist/
__pycache__/
*.pyc

# private research repo — never in the public tree
research/

# private partner-facing atlas (embeds research numbers + Evosub internals)
hub-explorer/

# replay captures — derived from third-party datasets, and large
apps/replay/out/

# brainflow captures — reproducible in one command, no reason to carry them
apps/brainflow/out/
